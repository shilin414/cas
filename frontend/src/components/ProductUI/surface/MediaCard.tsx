import type { ReactNode } from 'react';
import { Surface } from './Surface';
export interface MediaCardProps { media?: ReactNode; meta?: ReactNode; title: ReactNode; description?: ReactNode; tags?: ReactNode; actions?: ReactNode; onClick?: () => void; className?: string; ariaLabel?: string; }
export function MediaCard({ media, meta, title, description, tags, actions, onClick, className = '', ariaLabel }: MediaCardProps) { const content = <>{media && <div className="product-media-card__media">{media}</div>}<div className="product-media-card__body">{meta && <div className="product-media-card__meta">{meta}</div>}<h3>{title}</h3>{description && <p>{description}</p>}{tags && <div className="product-media-card__tags">{tags}</div>}{actions && <div className="product-media-card__actions">{actions}</div>}</div></>; return <Surface as="article" interactive={Boolean(onClick)} className={`product-media-card ${className}`.trim()}>{onClick ? <button type="button" className="product-media-card__action" onClick={onClick} aria-label={ariaLabel}>{content}</button> : content}</Surface>; }
