import type { ReactNode } from 'react';
export function EmptyState({ title = '暂无内容', description, action, icon, compact = false, className = '' }: { title?: ReactNode; description?: ReactNode; action?: ReactNode; icon?: ReactNode; compact?: boolean; className?: string }) { return <div className={`product-feedback-state${compact ? ' product-feedback-state--compact' : ''} ${className}`.trim()}>{icon && <div className="product-feedback-state__icon">{icon}</div>}<h2>{title}</h2>{description && <p>{description}</p>}{action && <div className="product-feedback-state__action">{action}</div>}</div>; }
