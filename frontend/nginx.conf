# Configuration is generated from ../deployment.json, not maintained by hand.
# Run `node scripts/deployment.mjs` from frontend/ to create nginx.generated.conf.
# Dockerfile installs that generated file and places dist under its basePath.
# See ../docs/deployment-subpath.md for non-Docker deployment instructions.
